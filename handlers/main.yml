- name: Reload "systemd"
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart "tailscale-proxy"
  ansible.builtin.systemd:
    name: tailscale-proxy
    state: restarted
    enabled: yes
